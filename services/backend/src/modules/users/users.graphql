# Types
type User {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
}

type Query {
  user(id: ID!): User
  allUsers:[User]
}
type Mutation {
  createUser(user: CreateUserInput): User
}

input CreateUserInput {
  firstName:String
  lastName:String
  email:String!
}